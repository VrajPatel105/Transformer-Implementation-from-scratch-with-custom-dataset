This folder contains transformer code from scratch without any help - Vraj

The order that will be followed : Embedding → Positional Encoding → Multi-Head Attention → Add & Norm → Feedforward → Add & Norm → stack into Encoder layer → Decoder (with cross-attention) → final Linear + Softmax.